% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization_report.R
\name{plot_microclimate_map}
\alias{plot_microclimate_map}
\title{Cartographier le microclimat}
\usage{
plot_microclimate_map(
  raster_data,
  type = "lst",
  layer_index = 1,
  title = NULL,
  palette = NULL,
  output_file = NULL,
  facet = FALSE
)
}
\arguments{
\item{raster_data}{SpatRaster à cartographier (LST, NDVI, anomalies, clusters)}

\item{type}{Type de carte: "lst", "ndvi", "anomaly", "cluster" (défaut: "lst")}

\item{layer_index}{Index de couche (défaut: 1)}

\item{title}{Titre de la carte (défaut: NULL = automatique)}

\item{palette}{Palette de couleurs (défaut: NULL = automatique selon type)}

\item{output_file}{Chemin pour sauvegarder (PNG/PDF, défaut: NULL)}

\item{facet}{Afficher toutes les couches (défaut: FALSE)}
}
\value{
Un objet ggplot2 invisible
}
\description{
Crée des cartes thématiques du microclimat: LST, anomalies thermiques,
îlots de chaleur, NDVI, zones de clustering.
}
\examples{
lst <- simulate_raster_data(type = "lst", n_dates = 3)
p   <- plot_microclimate_map(lst, type = "lst")

ndvi <- simulate_raster_data(type = "ndvi", n_dates = 1)
p2   <- plot_microclimate_map(ndvi, type = "ndvi")

}
