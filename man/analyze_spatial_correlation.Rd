% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_analysis.R
\name{analyze_spatial_correlation}
\alias{analyze_spatial_correlation}
\title{Analyser la corrélation spatiale (Moran's I)}
\usage{
analyze_spatial_correlation(
  features_df,
  variable = "lst",
  k_neighbours = 5,
  n_sample = 300
)
}
\arguments{
\item{features_df}{Data.frame avec colonnes x, y, et une variable cible}

\item{variable}{Nom de la variable à analyser (défaut: "lst")}

\item{k_neighbours}{Nombre de voisins pour la matrice de poids (défaut: 5)}

\item{n_sample}{Nombre de points max à utiliser (défaut: 300)}
}
\value{
Une liste avec: moran_i, p_value, interpretation, et le dataframe
}
\description{
Calcule l'indice de Moran's I pour détecter l'autocorrélation spatiale
dans les données thermiques. Un I positif indique des clusters thermiques,
un I négatif indique une dispersion.
}
\examples{
lst  <- simulate_raster_data(type = "lst", n_dates = 1)
features <- extract_microclimate_features(lst, n_sample = 200)
result <- analyze_spatial_correlation(features, variable = "lst")
print(result$interpretation)

}
