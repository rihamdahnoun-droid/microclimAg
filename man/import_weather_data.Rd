% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_data.R
\name{import_weather_data}
\alias{import_weather_data}
\title{Importer des données météorologiques}
\usage{
import_weather_data(
  filepath = NULL,
  date_col = "date",
  temp_col = "temperature",
  hum_col = "humidity",
  wind_col = "wind_speed",
  rad_col = "radiation",
  station_col = "station_id",
  fill_missing = "mean"
)
}
\arguments{
\item{filepath}{Chemin vers le fichier (CSV ou Excel .xlsx/.xls)}

\item{date_col}{Nom de la colonne date (défaut: "date")}

\item{temp_col}{Nom de la colonne température (défaut: "temperature")}

\item{hum_col}{Nom de la colonne humidité (défaut: "humidity")}

\item{wind_col}{Nom de la colonne vent (défaut: "wind_speed")}

\item{rad_col}{Nom de la colonne rayonnement (défaut: "radiation")}

\item{station_col}{Nom de la colonne station (défaut: "station_id")}

\item{fill_missing}{Méthode pour combler les NA: "mean", "linear", "none"
(défaut: "mean")}
}
\value{
Un data.frame avec les données météo standardisées
}
\description{
Importe des données météorologiques depuis un fichier CSV ou Excel.
Gère les dates, la fusion multi-stations et les données manquantes.
}
\examples{
# Utiliser des données simulées (sans fichier réel)
weather <- simulate_weather_data(n_days = 30, n_stations = 3)

# Ou depuis un fichier CSV:
# weather <- import_weather_data("mes_donnees.csv")

}
