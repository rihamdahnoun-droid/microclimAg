% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_analysis.R
\name{model_temperature_relationships}
\alias{model_temperature_relationships}
\title{Modéliser les relations microclimatiques}
\usage{
model_temperature_relationships(
  features_df,
  target = "lst",
  predictors = NULL,
  method = "lm"
)
}
\arguments{
\item{features_df}{Data.frame avec les variables microclimatiques}

\item{target}{Nom de la variable cible (défaut: "lst")}

\item{predictors}{Vecteur de noms de prédicteurs (défaut: NULL = auto)}

\item{method}{Méthode: "lm" (régression linéaire) (défaut: "lm")}
}
\value{
Une liste avec: model, summary, coefficients, r_squared, importance
}
\description{
Modélise la relation entre la température de surface (LST) et des
variables explicatives (NDVI, altitude, occupation du sol) par régression
linéaire.
}
\examples{
lst     <- simulate_raster_data(type = "lst",  n_dates = 1)
ndvi    <- simulate_raster_data(type = "ndvi", n_dates = 1)
lc      <- import_landcover()
features <- extract_microclimate_features(lst, ndvi, lc)
model    <- model_temperature_relationships(features)
print(model$coefficients)
cat("R² =", model$r_squared, "\n")

}
