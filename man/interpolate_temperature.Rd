% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_analysis.R
\name{interpolate_temperature}
\alias{interpolate_temperature}
\title{Interpoler la température spatialement}
\usage{
interpolate_temperature(
  stations_sf,
  temp_col = "temperature",
  method = "idw",
  resolution = 0.01,
  idw_power = 2
)
}
\arguments{
\item{stations_sf}{Objet sf avec les stations et températures}

\item{temp_col}{Nom de la colonne température dans stations_sf (défaut: "temperature")}

\item{method}{Méthode: "idw" ou "kriging" (défaut: "idw")}

\item{resolution}{Résolution de la grille de sortie en degrés (défaut: 0.01)}

\item{idw_power}{Puissance IDW (défaut: 2)}
}
\value{
Un SpatRaster de température interpolée
}
\description{
Interpole les températures des stations météo pour créer une surface
continue. Méthodes disponibles: IDW (Inverse Distance Weighting) ou
krigeage simplifié.
}
\examples{
# Créer des données de stations simulées
set.seed(42)
stations_df <- data.frame(
  station_id  = paste0("ST", 1:10),
  longitude   = runif(10, -5.5, -5.0),
  latitude    = runif(10, 33.5, 34.0),
  temperature = runif(10, 28, 40)
)
stations_sf <- sf::st_as_sf(stations_df, coords = c("longitude", "latitude"), crs = 4326)
temp_raster <- interpolate_temperature(stations_sf, method = "idw")
terra::plot(temp_raster, main = "Temperature interpolee IDW (°C)")

}
